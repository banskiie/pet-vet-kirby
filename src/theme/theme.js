import { createTheme } from "@mui/material";

export const baseTheme = createTheme({
  palette: {
    primary: {
      main: "#FF5733",
    },
    secondary: {
      main: "#FAC898",
    },
  },
  typography: {
    fontFamily: "Roboto",
  },
})
